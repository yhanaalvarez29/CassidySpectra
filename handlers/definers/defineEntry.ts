/**
 * Defines and returns a command entry.
 *
 * @param entry - The command entry to be defined.
 * @returns The defined command entry.
 */
export function defineEntry(entry: CommandEntry) {
  return entry;
}
